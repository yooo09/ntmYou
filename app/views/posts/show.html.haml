.edit.btn
  .post-content
    .post-content_box
      .post__ntmy-day
        = @post.created_at.strftime("%Y/%m/%d")
      .post__image
        = @post.image
      .post__animal
        = @post.animal
      .post__food
        = @post.food
      .post__aura_color
        = @post.aura_color
      .post__keyword1
        = @post.keyword1
      .post__keyword2
        = @post.keyword2
      .post__keyword3
        = @post.keyword3
  - if user_signed_in? && @post.user_id == current_user.id
    .edit-btn
      = link_to edit_post_path(@post.id) do
        編集する
    .addpost-btn
      = link_to new_post_addpost_path(@post.id) do
        追加する
    .delete-btn
      = link_to post_path(@post.id), method: :delete do
        削除する